// 2-Sum of N natrural numbers

let value = parseInt(prompt("Enter a number:"));
let sum = 0;
for (let i = 0; i < value; i++) {
    sum = sum + i;
    // console.log("sum numbers:" + sum);
}
console.log("sum numbers:" + sum);